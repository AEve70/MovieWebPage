export interface Movie {
    id: number;
    title: string;
    year: number;
    description: string;
    genre: string;
    rating: number;
    duration: number; // duration in minutes
    director: string;
    posterUrl: string;
}

